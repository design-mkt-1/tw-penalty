Thumbs.db
.DS_Store
*.log
__pycache__/

# Source renders that tools/cutout.py consumes. At fs-penalty these stayed
# tracked, 22 MB of them, in a public repo — still downloadable from
# raw.githubusercontent.com long after Pages was cleaned. The art is
# reproducible from the prompts in docs/ART.md, so the blobs never need to be.
raw/

# Placeholder art carried over from fs-penalty so the ported page runs while
# the Top Win art is being made. It wears FanSport's kit and does not belong in
# this repo's history. Etapa 3 replaces it and deletes this block -- when a
# `git status` here is clean of this line, the artwork is done.
#
# The other two placeholders are already gone. The ball came from a recipe in
# this repo and was simply repainted; the stadium plate has been regenerated
# and is tracked.
assets/img/keeper-*.webp
