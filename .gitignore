Thumbs.db
.DS_Store
*.log
__pycache__/

# Source renders that tools/cutout.py consumes. At fs-penalty these stayed
# tracked, 22 MB of them, in a public repo — still downloadable from
# raw.githubusercontent.com long after Pages was cleaned. The art is
# reproducible from the prompts in docs/ART.md, so the blobs never need to be.
raw/


# The local state file. It carries the offer terms and the decisions taken
# with the client; this repository is public. It is deliberately not the same
# document as docs/NEXT-SESSION.md, which is the version written for a reader
# outside the project.
am-nevoie-de-fix-whimsical-charm.md
